The `ReportManager` class in `workspace/report_manager.py` violates the Single Responsibility Principle. It handles both report data management AND formatting. Extract all formatting methods into a new `ReportFormatter` class in `workspace/report_formatter.py`. The `ReportManager` should keep data methods and use `ReportFormatter` for formatting. Update `report_manager.py` to import and use the new class.
